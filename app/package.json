{
  "name": "cigacount",
  "version": "1.0.0",
  "description": "Mobile-first web app that turns a smoker's reduction into savings toward a purchase goal.",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1",
    "helmet": "^8.3.0"
  }
}
